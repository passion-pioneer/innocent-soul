1. OS 커널 업데이트 등과 같이 변경작업이 있을 때 VM을 한대씩 교체하는 방향으로 하는가?
2. inline LB 에서 ingress 와 egress를 분리하여 독립적으로 생성하는 이유가 있는지?
3. TA 역할 : NCP 콘솔 작업, VM내에서 작업, 상황 대응, 네트워크 플로우 파악

### 질문 정리
4. NCP 에서 8비트 CIDR 을 제공하지 않는 이유
5. VM, FW, IPS, WAF, LB 등의 모든 자원이 사전에 구성되어 있다는 것을 전제로 거의 후속 작업으로 SFC 생성 ?
6. SFC 생성 절차
   1) VPC, Transit VPC 생성
   2) Connect 생성
   3) SFC Chain 생성
   4) Endpoint Routing Table 생성하고 Route 에서 Chain 등록
   5) Route table 에서 TVC 등록
7. 외부공개 도메인은 Global DNS 사용하고 내부 도메인도 있을텐데, 내부 도메인도 Global DNS 에서 관리 ? 
8. 동일 서브넷의 서버간 ACG 제어하는지? In/out any 로 열려있는지? Outbound 는 any 오픈
9.  DB/redis 도 IP 심을 수 있는지?
10. 테라폼으로 1차 배포후, 
    - ACG나 SFC와 관련된 RT 테이블 등을 정리해서 추가로 테라폼 배포 가능한지?


---

