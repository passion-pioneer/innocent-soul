### 질문 정리

1. OS 커널 업데이트 등과 같이 변경작업이 있을 때 VM을 한대씩 교체하는 방향으로 하는가?
2. inline LB 에서 ingress 와 egress를 분리하여 독립적으로 생성하는 이유가 있는지?
3. TA 역할 : NCP 콘솔 작업, VM내에서 작업, 상황 대응, 네트워크 플로우 파악
4. NCP 에서는 8비트 CIDR 을 제공하지 않는 이유
5. VM, FW, IPS, WAF, LB 등의 모든 자원이 사전에 구성되어 있다는 것을 전제로 거의 후속 작업으로 SFC 생성 맞는지?
6. SFC 생성 절차
   1) VPC, Transit VPC 생성
   2) Connect 생성
   3) SFC Chain 생성
   4) Endpoint Routing Table 생성하고 Route 에서 Chain 등록
   5) Route table 에서 TVC 등록
7. 외부공개 도메인은 Global DNS 사용하고 내부 도메인도 있을텐데, 어디에서 관리하는지?
8. 동일 서브넷의 서버간 ACG 제어하는지? In/out any 로 열려있는지?
9. DB/redis 도 IP 심을 수 있는지?
10. 테라폼으로 1차 배포후 SFC와 관련된 라우팅을 추가로 테라폼으로 배포 가능한지?
11.     

### 7/9 WBS
금주 서버 명세서 정리 후 이대원 책임에게 전달 (DS 인프라) ? 
차주 테라폼 코드 작성 및 리뷰 (LG CNS 이대원 책임) ?
계정게 호출 서버 GW 앞단에 분리 구축에 대한 명세(IP, 포트 등) 정리
신한 가맹점 세븐일레븐 확정


### GW 서버 추가건
계정계 호출 서버 일원화하는 작업
필요한 것


EWA, AWA, PDM, PDV 와 GW호출 서버간 내부 통신 포트
GW호출 서버와 프록시 서버간 통신 포트

GW호출 서버 Inbound (EWA, AWA, PDM, PDV)
GW호출 서버 Outbound (PROXY)

서브넷 대역
EWA, AWA, PDM, PDV, GW, PROXY 동일 대역
서버 2대 (c4-g2-s50)

정리될 것

ACG
서버 스펙

내부망에서의 통신 포트 확인
예)
10.217.6.0/23 서브넷 안의 ACG는 서버별로 제어하는지? 서브넷으로 열려있는지?
internal proxy lb 의 역할

