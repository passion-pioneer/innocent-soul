최초 테라폼 배포를 제외하고 손으로 하는 서비스들 나열

---

OS 커널 등과 같이 변경작업이 있을 때 VM을 한대씩 교체하는 방향으로 하는가?

inline LB 는 Transit VPC 내에서 사용
inline LB 에서 ingress 와 egress 역할을 분리하여 독립적으로 생성하는 이유가 있는지?


VPC
Load Balancer
Global DNS
IPsec VPN
Server
Storage
Database
Cloud Data Streaming Service
Management & Governance

주로 네트워킹, 서버, DB, 스토리지


TA가 하는 역할
NCP 콘솔에서의 구성, OS 기동 후 작업들을 전제로 하고 네트워킹 흐름을 아는 것

chain 은 어떤 장비의 흐름을 탈 것인지 정의
endpoint routing table 에서 chain 을 등록하여 특정 네트워크나 ip는 특정 chain 으로 라우팅되도록 등록

---

1. Transit VPC 생성
        │
2. Transit VPC Connect(TVC) 생성
        │
3. Service Function(FW, IPS, WAF 등) 배포
        │
4. SFC Chain 생성
        │
5. Endpoint Routing Table 생성
        │
6. Endpoint Routing Table에 Chain 연결
        │
7. VPC Route Table에서 Transit VPC Endpoint로 라우팅

---

SFC를 사용하기 위해서는
VM, FW, IPS, WAF, LB 등이 사전에 구성되어 있어야 하고

모든 인프라가 사전 구성되어있는 상태에서 거의 마지막 구성

1.VPC, Transit VPC 생성
2.Connect 생성
3.SFC Chain 생성
4.Endpoint Routing Table 생성하고 Route 에서 Chain 등록
5.Route table 에서 TVC 등록


---

8비트 CIDR 제공안하는 이유
